# FocusLock v1.1.0 - Requirements
# Core app runs on standard library only (Python 3.10+)

# Optional - for system tray icon support (not needed with PySide6, but keeping for reference)
pystray>=0.19.0
Pillow>=10.0.0

# UI Framework
PySide6>=6.5.0
psutil>=5.9.0

# Optional - for building a standalone .exe
pyinstaller>=6.0
